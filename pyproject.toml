[tool.pytest.ini_options]
# Put the repo root on sys.path so `import src` works under plain `pytest`
# (not just `python -m pytest`), which is how CI invokes it.
pythonpath = ["."]
testpaths = ["tests"]
